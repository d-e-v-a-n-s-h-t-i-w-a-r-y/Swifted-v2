export function GlobalHeader() {
    return (
        <header className="mb-5">
            <h1 className="text-[19px] font-semibold text-foreground leading-tight tracking-tight">
                SwiftEd
            </h1>
            <p className="text-[15px] font-bold mt-1.5 bg-gradient-to-r from-primary via-accent to-primary bg-clip-text text-transparent">
                Small steps. Real progress.
            </p>
        </header>
    );
}
